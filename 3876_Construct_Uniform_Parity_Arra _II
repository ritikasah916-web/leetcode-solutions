class Solution:
 def uniformArray(self, nums1: list[int]) -> bool:
    min_val = min(nums1)
    min_odd = min((x for x in nums1 if x % 2 != 0), default=float("inf"))

    # True if all are even, or if the overall minimum element is odd
    return min_odd == float("inf") or min_val == min_odd       