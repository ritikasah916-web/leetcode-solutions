class Solution:
    def smallestIndex(self, nums: List[int]) -> int:
        for i, nums in enumerate(nums):
            digit_sum = 0

            while nums:
                digit_sum += nums % 10
                nums //= 10

            if digit_sum == i:
                return i

        return -1            
        